-- animation.lua --

animations = {
  {
    name = "runner",
    x = 34,
    y = 48,
    w = 102,
    h = 96,
    anims = {
      {'1-3', '1-2', speed = }
    }
  }
}

function getAnimation(name, image)
  --[[
  local retImg = love.graphics.newImage(image)
  local retGrid = anim8.newGrid(34, 48, 102, 480, 0, 0, 0) --432
  
  ]]

  -- return retImg, retGrid, retAnimations
end
